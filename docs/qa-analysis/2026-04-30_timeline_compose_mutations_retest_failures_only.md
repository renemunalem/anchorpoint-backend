# QA Retest Report — TimelineCompose Mutations (Failures Only)

**Date:** 2026-04-30
**Tester:** Gemini QA
**Status:** ❌ **FAIL**
**Environment:**
- Frontend: http://localhost:5174
- Backend: http://localhost:3000
- Driver: `REPO_DRIVER=mysql`

## Summary
End-to-end smoke testing of the `TimelineCompose` panel was performed against the real MySQL backend after the `authenticatedFetch` header fix. The duplicated `Content-Type` issue has been resolved in the frontend. However, almost all mutations are currently failing with backend-side errors (`500 Internal Server Error` and `400 Bad Request`).

## Test Steps
1. Logged in as `admin@atlasai.local`.
2. Opened Case `500Hu00002SXHJtIAP` (Awaiting Reply scenario).
3. Verified HIPAA (member access).
4. Attempted all available Compose actions.

## Failures

### 1. [P0] Save Note Fails (500)
- **Action:** Compose > Note > Fill text > Save Note.
- **Expected:** 200 OK, timeline updates.
- **Actual:** `500 Internal Server Error`.
- **Backend Route:** `POST /api/v1/cases/500Hu00002SXHJtIAP/notes`
- **Owner:** Backend (Codex)
- **Repro / Error:** The request payload is correct (`{"text": "..."}`). The MySQL repository throws an exception during the database transaction causing a rollback.

### 2. [P0] Log Call Fails (500)
- **Action:** Compose > Call Log > Fill summary/outcome > Log Call.
- **Expected:** 200 OK, timeline updates.
- **Actual:** `500 Internal Server Error`.
- **Backend Route:** `POST /api/v1/cases/500Hu00002SXHJtIAP/calls`
- **Owner:** Backend (Codex)

### 3. [P0] Create Task Fails (500)
- **Action:** Compose > + Task > Fill description > Create Task.
- **Expected:** 200 OK, timeline updates.
- **Actual:** `500 Internal Server Error`.
- **Backend Route:** `POST /api/v1/cases/500Hu00002SXHJtIAP/tasks`
- **Owner:** Backend (Codex)

### 4. [P0] Send Email Fails (400)
- **Action:** Compose > Email > Fill Subject and Body > Send.
- **Expected:** 200 OK, timeline updates.
- **Actual:** `400 Bad Request`.
- **Backend Route:** `POST /api/v1/cases/500Hu00002SXHJtIAP/emails`
- **Owner:** Backend (Codex) / Frontend (Claude)
- **Repro / Error:** The frontend is seemingly sending requests that trigger validation errors on the backend, or the backend expects fields differently than they are supplied.

### 5. [P1] Patch Status Fails (500)
- **Action:** Click "Waiting" from the quick status bar.
- **Expected:** 200 OK, case status updates.
- **Actual:** `500 Internal Server Error`.
- **Backend Route:** `PATCH /api/v1/cases/500Hu00002SXHJtIAP`
- **Owner:** Backend (Codex)

### 6. [P1] Patch Agent Fails (500)
- **Action:** Select an agent from the "Assign agent" dropdown.
- **Expected:** 200 OK, case assignment updates.
- **Actual:** `500 Internal Server Error`.
- **Backend Route:** `PATCH /api/v1/cases/500Hu00002SXHJtIAP`
- **Owner:** Backend (Codex)

### 7. [P0] Close Case Fails (500)
- **Action:** Compose > Close Case > Fill resolution > Submit.
- **Expected:** 200 OK, case status updates to closed.
- **Actual:** `500 Internal Server Error`.
- **Backend Route:** `POST /api/v1/cases/500Hu00002SXHJtIAP/close`
- **Owner:** Backend (Codex)

## Notes
- **UI Behavior:** The UI properly handles the HTTP responses and no longer crashes. Generic crash states have been eliminated, though the actions themselves still fail to persist on the backend.
- The duplicated `Content-Type: application/json, application/json` issue is confirmed resolved on the frontend; requests are now sent with a singular, correct header.

---
*Report generated by Gemini QA Agent*