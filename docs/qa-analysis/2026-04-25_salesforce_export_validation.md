# Salesforce Export Validation Report - AtlasAI Migration

**Date:** 2026-04-25
**Tester:** Gemini (QA-only)
**Status:** ⚠️ Partial Readiness (Binary files present; encoding and linkage planning still required)

## 1. Directory Verification
All key CSV files identified in the Codex mapping report are present in `/imports/salesforce/`.

| File | Status | Record Count (wc -l) |
| :--- | :--- | :--- |
| **Case.csv** | ✅ Present | 11,459 |
| **Contact.csv** | ✅ Present | 8,269 |
| **Account.csv** | ✅ Present | 667 |
| **User.csv** | ✅ Present | 51 |
| **CaseHistory2.csv** | ✅ Present | 24,101 |
| **EmailMessage.csv** | ✅ Present | 2,692 |
| **Task.csv** | ✅ Present | 1,058 |
| **FeedPost.csv** | ✅ Present | 3,112 |
| **ContentDocumentLink.csv** | ✅ Present | 2,146 |
| **ContentVersion.csv** | ✅ Present | 885 |

The newer dated export also includes binary payloads and export-specific metadata under `/imports/salesforce/exports/2026-04-25/`:

| Export asset | Status | Count |
| :--- | :--- | ---: |
| **Attachment.csv** | ✅ Present | 16 lines / 15 data rows |
| **ContentDocumentLink.csv** | ✅ Present | 2,180 lines / 2,179 data rows |
| **ContentVersion.csv** | ✅ Present | 894 lines / 893 data rows |
| **Attachments/** | ✅ Present | 15 binary files |
| **ContentVersion/** | ✅ Present | 893 binary files |

## 2. Critical Findings

### [P1] Attachment binaries are present, but case-link mapping is not defined yet
* **Severity:** P1
* **Finding:** The new dated export includes payload folders plus matching metadata CSVs, so the previous “missing binaries” blocker is no longer valid. The remaining work is to define how AtlasAI will link `Attachment.csv` and `ContentVersion.csv` payloads back to cases and case-related entities.
* **Evidence:** `imports/salesforce/exports/2026-04-25/ContentVersion/` contains 893 files and `Attachment.csv` contains 15 data rows matching the 15 files under `Attachments/`.
* **Impact:** Migration is no longer blocked on missing files, but attachment/document linkage still needs a backend contract and mapping plan before importer implementation starts.

### [P1] Non-UTF8 Encoding (ISO-8859-1)
* **Severity:** P1
* **Finding:** Several critical files use `iso-8859-1` encoding instead of `UTF-8`.
* **Evidence:** `file -I Case.csv` returns `charset=iso-8859-1`.
* **Impact:** Non-ASCII characters (smart quotes, em-dashes, special symbols) appear mangled. Example observed in `Case.csv`: `J0775`, `year`, `pounds `.
* **Affected Files:** `Case.csv`, `Task.csv`, `FeedPost.csv`, `ContentVersion.csv`.

## 3. Data Integrity & Volume
The export contains a substantial amount of data, making it excellent for testing the migration logic:
* **Relational Integrity:** Headers confirm `ParentId`, `ContactId`, `AccountId`, and `OwnerId` are present across Case, EmailMessage, and History objects.
* **Rich Metadata:** `Case.csv` includes over 80 columns, covering both standard and custom fields (e.g., `Member_ID__c`, `Amount_Billed__c`).

## 4. MVP Readiness: **PARTIAL**
* **Start MVP Import?** Yes (for structured data).
* **Why:** The CSV data for Cases, Contacts, and Accounts is sufficient to build the core AtlasAI database, and the dated export now includes binary file payloads for both legacy attachments and `ContentVersion` records. The remaining gaps are attachment-to-case linkage planning and the encoding issue.

## 5. Next Best Developer Prompt

### For Codex (Backend/Migration)
> "Update the AtlasAI backend migration plan to use the dated export at `imports/salesforce/exports/2026-04-25/` as the source of truth for file payloads. Define how `Attachment.csv`, `ContentDocumentLink.csv`, and `ContentVersion.csv` map binary files back to cases and case-related records, and keep UTF-8 normalization as a required safety rule before any importer work."

---
*Generated by Gemini QA*
